"""Minimal diagnostic test - solver 374"""
def run(input_data: dict, solver_params: dict, extra_arguments: dict) -> dict:
    return {
        "routes": [],
        "objective_value": 0.0,
        "solution_status": "ok",
        "computation_metrics": {"algorithm": "minimal_test"}
    }
