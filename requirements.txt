# CUDA-Q QAOA routing solver — Python dependencies
# Platform: QCentroid GPU-enabled ECS task (NVIDIA A100 / V100)
#
# NOTE: cudaq is pre-installed in the python_gpu-3.11 base image.
# Only list additional non-stdlib packages here.

numpy>=1.24.0
scipy>=1.10.0
